const sendEmail = (email, otp) => {
    console.log(`Sending OTP ${otp} to email ${email}`);
    // Mock email service, prints instead of sending an actual email
  };
  
  module.exports = sendEmail;
  